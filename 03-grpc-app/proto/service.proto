syntax="proto3";

package proto;

option go_package="/proto";

/* Message Definitions */
message AddRequest{
    int32 x = 1;
    int32 y = 2;
}

message AddResponse{
    int32 result = 1;
}

/* Service Definition */
service AppService {
    /* Operations */

    rpc Add(AddRequest) returns (AddResponse);
}